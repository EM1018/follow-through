{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uv run sh -c \"alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT\"",
    "healthcheckPath": "/health"
  }
}
