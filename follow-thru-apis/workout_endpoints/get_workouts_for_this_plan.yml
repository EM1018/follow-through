info:
  name: get_workouts_for_this_plan
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8000/plans/{plan-id}/workouts
  auth:
    type: bearer
    token: "{{token_test2_user}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
