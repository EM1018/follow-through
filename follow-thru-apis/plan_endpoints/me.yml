info:
  name: me
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:8000/me
  auth:
    type: bearer
    token: "{{token_test1_user}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
