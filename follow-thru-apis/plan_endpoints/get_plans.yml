info:
  name: get_plans
  type: http
  seq: 7

http:
  method: GET
  url: http://127.0.0.1:8000/plans
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token_test2_user}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
